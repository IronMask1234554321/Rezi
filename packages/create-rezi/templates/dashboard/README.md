# __APP_NAME__

Scaffolded with `npm create rezi` using the **__TEMPLATE_LABEL__** template.

## Quickstart

```bash
npm install
npm start
```

## Keybindings

- `up` / `down` or `j` / `k`: Move selection
- `f`: Cycle filter
- `enter`: Pin service
- `?`: Toggle help
- `q`: Quit
