# __APP_NAME__

Scaffolded with `npm create rezi` using the **__TEMPLATE_LABEL__** template.

## Quickstart

```bash
npm install
npm start
```

## Keybindings

- `up` / `down` or `j` / `k`: Switch stream
- `space`: Toggle pause
- `f`: Follow mode
- `q`: Quit
