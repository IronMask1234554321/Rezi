# Versioning

Rezi has multiple version pins:

- engine ABI (Zireael)
- drawlist format version(s)
- event batch format version(s)
- pinned Unicode tables for text measurement

These are exported from `@rezi-ui/core` so wrappers and tooling can validate compatibility.

See also:

- [ABI pins](abi.md)
